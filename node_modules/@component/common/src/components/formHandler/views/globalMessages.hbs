<div class="{{msgClass}}">
    <ul>
        {{#each errors}} {{#condition "@index === 0" }}
        <li autofocus tabindex="-1">{{{this}}}</li>
        {{else}}
        <li>{{this}}</li>
        {{/condition}} {{/each}}

{{!--
        {{#each errors}}
            <li><a href="#{{id}}">{{description}}</a></li>
        {{/each}}
--}}

    </ul>
</div>
