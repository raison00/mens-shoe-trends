const pkg = require('./pkg');

module.exports = () => {
  let name;

  try {
    name = pkg().name.split('/')[1]; // eslint-disable-line prefer-destructuring
  } catch (err) {
    throw new Error('Invalid package name');
  }

  return name;
};
