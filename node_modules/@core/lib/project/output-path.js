const name = require('./name');
const pkg = require('./pkg');
const type = require('./type');
const resolve = require('./resolve');

module.exports = (entryPoint, version) => {
  const basePath = (entryPoint) ? (`dist/${entryPoint}`) : ('dist');
  return resolve(`${basePath}/${type()}/${name()}/${version || pkg().version}`);
};
