// Function should be called from a project's cwd() in order to resolve correctly

module.exports = () => ({
  name: require('./name')(),
  pkg: require('./pkg')(),
  type: require('./type')(),
  outputPath: require('./output-path'),
  publicPath: require('./public-path'),
  resolve: require('./resolve'),
  modules: require('./modules'),
});
