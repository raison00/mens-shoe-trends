/**
 * @file pkg.js
 * Return the active project's package.json
 * Called pkg instead of package to all importing of a non-reserved word
 */
const resolve = require('./resolve');

module.exports = () => require(resolve('package.json')); // eslint-disable-line import/no-dynamic-require
