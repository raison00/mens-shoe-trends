const pkg = require('./pkg');

module.exports = () => {
  let type;

  try {
    type = pkg().name.split('/')[0].substring(1);
  } catch (err) {
    throw new Error('Invalid package name');
  }

  return type;
};
