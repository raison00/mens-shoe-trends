const path = require('path');

const modules = [
  path.resolve(__dirname, '../../vendor/node_modules'), // shared vendor modules
  path.resolve(__dirname, '../../..'), // shared build modules and project modules (flattened)
  'node_modules', // project-specific modules not already captured
];

module.exports = modules;
