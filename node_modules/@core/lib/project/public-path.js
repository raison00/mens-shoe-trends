const name = require('./name');
const pkg = require('./pkg');
const type = require('./type');
const basePath = require('./base');

module.exports = (version) => {
  const p = pkg();
  return `${basePath}/${type()}/${name()}/${version || p.version}/`;
};
