/**
 * @file path.js
 * Resolve a filepath relative to the project path of the current working directory.
 */
const path = require('path');
const pkgDir = require('pkg-dir');

module.exports = (...paths) => path.resolve(pkgDir.sync() || process.cwd(), ...paths);
