require(`./src/scss/${process.env.ENTRY_POINT}/${process.env.ENTRY_POINT}-sprites.scss`);

const App = {
  start() {
    // document.addEventListener('DOMContentLoaded', () => {
    //   const temp = document.createElement('b');
    //   temp.innerHTML = ' cat';
    //   document.getElementsByClassName('foo')[0].appendChild(temp);
    // });
  },
};


export default App;
